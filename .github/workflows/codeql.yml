name: CodeQL

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "0 6 * * 1"

jobs:
  analyze:
    name: Analyze
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: [go, javascript]

    steps:
      - name: Checkout
        uses: asymmetric-effort/actions/actions/checkout@940ebd094488ead7765982278cfda3653fdbce48 # v1.0.9

      - name: Initialize CodeQL
        uses: asymmetric-effort/actions/actions/codeql-init@940ebd094488ead7765982278cfda3653fdbce48 # v1.0.9
        with:
          languages: ${{ matrix.language }}

      - name: Autobuild
        uses: asymmetric-effort/actions/actions/codeql-autobuild@940ebd094488ead7765982278cfda3653fdbce48 # v1.0.9

      - name: Perform CodeQL Analysis
        uses: asymmetric-effort/actions/actions/codeql-analyze@940ebd094488ead7765982278cfda3653fdbce48 # v1.0.9
        with:
          category: "/language:${{ matrix.language }}"
